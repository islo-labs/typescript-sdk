// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("WebhooksClient", () => {
    test("list_incoming_webhooks (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = [
            {
                auth: { auth_type: "none" },
                created_at: "created_at",
                id: "id",
                idempotency: { source: "header", name: "name" },
                name: "name",
                receiver_url: "receiver_url",
                rules: [
                    {
                        actions: [
                            {
                                action_type: "ensure_sandbox",
                                template: { disk_gb: 1, image: "image", memory_mb: 1, vcpus: 1 },
                            },
                        ],
                    },
                ],
                status: "active",
                target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
            },
        ];

        server.mockEndpoint().get("/webhooks/incoming").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.webhooks.listIncomingWebhooks();
        expect(response).toEqual(rawResponseBody);
    });

    test("list_incoming_webhooks (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server.mockEndpoint().get("/webhooks/incoming").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.webhooks.listIncomingWebhooks();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("create_incoming_webhook (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {
            auth: { auth_type: "none" },
            idempotency: { source: "header", name: "name" },
            name: "name",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };
        const rawResponseBody = {
            auth: { auth_type: "none" },
            created_at: "created_at",
            id: "id",
            idempotency: { source: "header", name: "name" },
            name: "name",
            receiver_url: "receiver_url",
            rules: [
                {
                    actions: [
                        {
                            action_type: "ensure_sandbox",
                            template: { disk_gb: 1, image: "image", memory_mb: 1, vcpus: 1 },
                        },
                    ],
                },
            ],
            status: "active",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };

        server
            .mockEndpoint()
            .post("/webhooks/incoming")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.webhooks.createIncomingWebhook({
            auth: {
                auth_type: "none",
            },
            idempotency: {
                source: "header",
                name: "name",
            },
            name: "name",
            target: {
                target_type: "fixed_sandbox_name",
                sandbox_name: "sandbox_name",
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("create_incoming_webhook (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {
            auth: { auth_type: "none" },
            idempotency: { source: "header", name: "name" },
            name: "name",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .post("/webhooks/incoming")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.createIncomingWebhook({
                auth: {
                    auth_type: "none",
                },
                idempotency: {
                    source: "header",
                    name: "name",
                },
                name: "name",
                target: {
                    target_type: "fixed_sandbox_name",
                    sandbox_name: "sandbox_name",
                },
            });
        }).rejects.toThrow(IsloApi.BadRequestError);
    });

    test("create_incoming_webhook (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {
            auth: { auth_type: "none" },
            idempotency: { source: "header", name: "name" },
            name: "name",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .post("/webhooks/incoming")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.createIncomingWebhook({
                auth: {
                    auth_type: "none",
                },
                idempotency: {
                    source: "header",
                    name: "name",
                },
                name: "name",
                target: {
                    target_type: "fixed_sandbox_name",
                    sandbox_name: "sandbox_name",
                },
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_incoming_webhook (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            auth: { auth_type: "none" },
            created_at: "created_at",
            id: "id",
            idempotency: { source: "header", name: "name" },
            name: "name",
            receiver_url: "receiver_url",
            rules: [
                {
                    actions: [
                        {
                            action_type: "ensure_sandbox",
                            template: { disk_gb: 1, image: "image", memory_mb: 1, vcpus: 1 },
                        },
                    ],
                },
            ],
            status: "active",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };

        server
            .mockEndpoint()
            .get("/webhooks/incoming/webhook_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.webhooks.getIncomingWebhook({
            webhook_id: "webhook_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_incoming_webhook (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .get("/webhooks/incoming/webhook_id")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.getIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_incoming_webhook (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/webhooks/incoming/webhook_id")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.getIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("delete_incoming_webhook (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().delete("/webhooks/incoming/webhook_id").respondWith().statusCode(200).build();

        const response = await client.webhooks.deleteIncomingWebhook({
            webhook_id: "webhook_id",
        });
        expect(response).toEqual(undefined);
    });

    test("delete_incoming_webhook (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .delete("/webhooks/incoming/webhook_id")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.deleteIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("delete_incoming_webhook (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .delete("/webhooks/incoming/webhook_id")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.deleteIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("update_incoming_webhook (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = {
            auth: { auth_type: "none" },
            created_at: "created_at",
            id: "id",
            idempotency: { source: "header", name: "name" },
            name: "name",
            receiver_url: "receiver_url",
            rules: [
                {
                    actions: [
                        {
                            action_type: "ensure_sandbox",
                            template: { disk_gb: 1, image: "image", memory_mb: 1, vcpus: 1 },
                        },
                    ],
                },
            ],
            status: "active",
            target: { target_type: "fixed_sandbox_name", sandbox_name: "sandbox_name" },
        };

        server
            .mockEndpoint()
            .patch("/webhooks/incoming/webhook_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.webhooks.updateIncomingWebhook({
            webhook_id: "webhook_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("update_incoming_webhook (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .patch("/webhooks/incoming/webhook_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.updateIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.BadRequestError);
    });

    test("update_incoming_webhook (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .patch("/webhooks/incoming/webhook_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.updateIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("update_incoming_webhook (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .patch("/webhooks/incoming/webhook_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.webhooks.updateIncomingWebhook({
                webhook_id: "webhook_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });
});
