// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("GatewayInternalClient", () => {
    test("get_gateway_policy_by_key (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/internal/gateway/policy/profile_key")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.gatewayInternal.getGatewayPolicyByKey({
            profile_key: "profile_key",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_gateway_policy_by_key (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/internal/gateway/policy/profile_key")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.gatewayInternal.getGatewayPolicyByKey({
                profile_key: "profile_key",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });
});
