// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SandboxesClient", () => {
    test("list_exec_sessions (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = [
            {
                exec_id: "exec_id",
                is_interactive: true,
                first_timestamp: "2024-01-15T09:30:00Z",
                last_timestamp: "2024-01-15T09:30:00Z",
                log_count: 1,
                first_stdin_line: "first_stdin_line",
            },
        ];

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.listExecSessions({
            sandbox_id: "sandbox_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list_exec_sessions (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listExecSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("list_exec_sessions (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listExecSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("list_exec_sessions (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listExecSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("get_exec_session_asciinema (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/asciinema")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.getExecSessionAsciinema({
            sandbox_id: "sandbox_id",
            exec_id: "exec_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_exec_session_asciinema (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/asciinema")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionAsciinema({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_exec_session_asciinema (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/asciinema")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionAsciinema({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("get_exec_session_asciinema (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/asciinema")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionAsciinema({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("get_exec_session_logs (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            exec_id: "exec_id",
            logs: [{ timestamp: "2024-01-15T09:30:00Z", seq: 1, stream: "stdout", data: "data" }],
        };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/logs")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.getExecSessionLogs({
            sandbox_id: "sandbox_id",
            exec_id: "exec_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_exec_session_logs (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/logs")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionLogs({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_exec_session_logs (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/logs")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionLogs({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("get_exec_session_logs (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/exec-sessions/exec_id/logs")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getExecSessionLogs({
                sandbox_id: "sandbox_id",
                exec_id: "exec_id",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("list_agent_sessions (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = [
            {
                session_path: "session_path",
                session_name: "session_name",
                parent_session_path: "parent_session_path",
                parent_session_name: "parent_session_name",
                root_session_path: "root_session_path",
                root_session_name: "root_session_name",
                is_subagent: true,
                session_depth: 1,
                agent_id: "agent_id",
                child_count: 1,
                first_timestamp: "2024-01-15T09:30:00Z",
                last_timestamp: "2024-01-15T09:30:00Z",
                event_count: 1,
                model: "model",
                total_input_tokens: 1,
                total_output_tokens: 1,
                cwd: "cwd",
                git_branch: "git_branch",
                harness_name: "harness_name",
                total_cache_creation_tokens: 1,
                total_cache_read_tokens: 1,
                first_user_text: "first_user_text",
            },
        ];

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.listAgentSessions({
            sandbox_id: "sandbox_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("list_agent_sessions (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listAgentSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("list_agent_sessions (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listAgentSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("list_agent_sessions (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.listAgentSessions({
                sandbox_id: "sandbox_id",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("get_agent_session_events (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = [
            {
                session_path: "session_path",
                session_name: "session_name",
                parent_session_path: "parent_session_path",
                parent_session_name: "parent_session_name",
                root_session_path: "root_session_path",
                root_session_name: "root_session_name",
                is_subagent: true,
                session_depth: 1,
                agent_id: "agent_id",
                timestamp: "2024-01-15T09:30:00Z",
                event_type: "event_type",
                actor: "actor",
                model: "model",
                input_tokens: 1,
                output_tokens: 1,
                harness_name: "harness_name",
                cwd: "cwd",
                body: "body",
                cache_creation_input_tokens: 1,
                cache_read_input_tokens: 1,
            },
        ];

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions/session_name/events")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.getAgentSessionEvents({
            sandbox_id: "sandbox_id",
            session_name: "session_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_agent_session_events (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions/session_name/events")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getAgentSessionEvents({
                sandbox_id: "sandbox_id",
                session_name: "session_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_agent_session_events (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions/session_name/events")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getAgentSessionEvents({
                sandbox_id: "sandbox_id",
                session_name: "session_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("get_agent_session_events (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_id/agent-sessions/session_name/events")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getAgentSessionEvents({
                sandbox_id: "sandbox_id",
                session_name: "session_name",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("list_sandboxes (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            items: [
                {
                    created_at: "created_at",
                    created_by: "created_by",
                    created_by_entity: "created_by_entity",
                    deleted_at: "deleted_at",
                    id: "id",
                    image: "image",
                    name: "name",
                    owner_node_id: "owner_node_id",
                    spec: { disk_gb: 1, memory_mb: 1, vcpus: 1 },
                    status: "status",
                    vm_id: "vm_id",
                    workdir: "workdir",
                },
            ],
            limit: 1,
            offset: 1,
            total: 1,
        };

        server.mockEndpoint().get("/sandboxes").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.sandboxes.listSandboxes();
        expect(response).toEqual(rawResponseBody);
    });

    test("list_sandboxes (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server.mockEndpoint().get("/sandboxes").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.sandboxes.listSandboxes();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("create_sandbox (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = {
            created_at: "created_at",
            created_by: "created_by",
            created_by_entity: "created_by_entity",
            deleted_at: "deleted_at",
            id: "id",
            image: "image",
            name: "name",
            owner_node_id: "owner_node_id",
            setup_steps: [{ key: "value" }],
            spec: { disk_gb: 1, memory_mb: 1, vcpus: 1 },
            status: "status",
            vm_id: "vm_id",
            workdir: "workdir",
        };

        server
            .mockEndpoint()
            .post("/sandboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.createSandbox();
        expect(response).toEqual(rawResponseBody);
    });

    test("create_sandbox (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.createSandbox();
        }).rejects.toThrow(IsloApi.BadRequestError);
    });

    test("create_sandbox (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.createSandbox();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("create_sandbox (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.createSandbox();
        }).rejects.toThrow(IsloApi.ConflictError);
    });

    test("create_sandbox (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = {};
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(503)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.createSandbox();
        }).rejects.toThrow(IsloApi.ServiceUnavailableError);
    });

    test("get_sandbox (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            created_at: "created_at",
            created_by: "created_by",
            created_by_entity: "created_by_entity",
            deleted_at: "deleted_at",
            id: "id",
            image: "image",
            name: "name",
            owner_node_id: "owner_node_id",
            setup_steps: [{ key: "value" }],
            spec: { disk_gb: 1, memory_mb: 1, vcpus: 1 },
            status: "status",
            vm_id: "vm_id",
            workdir: "workdir",
        };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.getSandbox({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_sandbox (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getSandbox({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_sandbox (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.getSandbox({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("delete_sandbox (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().delete("/sandboxes/sandbox_name").respondWith().statusCode(200).build();

        const response = await client.sandboxes.deleteSandbox({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(undefined);
    });

    test("delete_sandbox (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .delete("/sandboxes/sandbox_name")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.deleteSandbox({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("delete_sandbox (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .delete("/sandboxes/sandbox_name")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.deleteSandbox({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_exec_interactive (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/exec").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxExecInteractive({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_exec_interactive (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/exec")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxExecInteractive({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_exec_interactive (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/exec")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxExecInteractive({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_exec_stream (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { args: ["args"] };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/exec/stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.sandboxes.sandboxExecStream({
            sandbox_name: "sandbox_name",
            args: ["args"],
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_exec_stream (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { args: ["args", "args"] };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/exec/stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxExecStream({
                sandbox_name: "sandbox_name",
                args: ["args", "args"],
            });
        }).rejects.toThrow(IsloApi.BadRequestError);
    });

    test("sandbox_exec_stream (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { args: ["args", "args"] };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/exec/stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxExecStream({
                sandbox_name: "sandbox_name",
                args: ["args", "args"],
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_exec_stream (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { args: ["args", "args"] };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/exec/stream")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxExecStream({
                sandbox_name: "sandbox_name",
                args: ["args", "args"],
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_download_file (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/files").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxDownloadFile({
            sandbox_name: "sandbox_name",
            path: "path",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_download_file (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/files")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxDownloadFile({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_download_file (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/files")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxDownloadFile({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_upload_file (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { status: "status" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxUploadFile({
            sandbox_name: "sandbox_name",
            path: "path",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_upload_file (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxUploadFile({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_upload_file (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxUploadFile({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_download_archive (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/files-archive").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxDownloadArchive({
            sandbox_name: "sandbox_name",
            path: "path",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_download_archive (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/files-archive")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxDownloadArchive({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_download_archive (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/files-archive")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxDownloadArchive({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_upload_archive (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { status: "status" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files-archive")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxUploadArchive({
            sandbox_name: "sandbox_name",
            path: "path",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_upload_archive (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files-archive")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxUploadArchive({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_upload_archive (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/files-archive")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxUploadArchive({
                sandbox_name: "sandbox_name",
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_pause (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            created_at: "created_at",
            created_by: "created_by",
            created_by_entity: "created_by_entity",
            deleted_at: "deleted_at",
            id: "id",
            image: "image",
            name: "name",
            owner_node_id: "owner_node_id",
            setup_steps: [{ key: "value" }],
            spec: { disk_gb: 1, memory_mb: 1, vcpus: 1 },
            status: "status",
            vm_id: "vm_id",
            workdir: "workdir",
        };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/pause")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxPause({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_pause (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/pause")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPause({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_pause (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/pause")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPause({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_pause (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/pause")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPause({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.ConflictError);
    });

    test("sandbox_port_forward (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/port-forward").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxPortForward({
            sandbox_name: "sandbox_name",
            port: 1,
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_port_forward (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/port-forward")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPortForward({
                sandbox_name: "sandbox_name",
                port: 1,
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_port_forward (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/port-forward")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPortForward({
                sandbox_name: "sandbox_name",
                port: 1,
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_promote_cache (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { cache_key: "cache_key", status: "status", tenant_id: "tenant_id" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/promote-cache")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxPromoteCache({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_promote_cache (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/promote-cache")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPromoteCache({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_promote_cache (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/promote-cache")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxPromoteCache({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_proxy_root (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/proxy/1").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxProxyRoot({
            sandbox_name: "sandbox_name",
            port: 1,
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_proxy_root (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/proxy/1")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxProxyRoot({
                sandbox_name: "sandbox_name",
                port: 1,
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_proxy_root (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/proxy/1")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxProxyRoot({
                sandbox_name: "sandbox_name",
                port: 1,
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_proxy (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/proxy/1/path").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxProxy({
            sandbox_name: "sandbox_name",
            port: 1,
            path: "path",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_proxy (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/proxy/1/path")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxProxy({
                sandbox_name: "sandbox_name",
                port: 1,
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_proxy (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/proxy/1/path")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxProxy({
                sandbox_name: "sandbox_name",
                port: 1,
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_resume (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            created_at: "created_at",
            created_by: "created_by",
            created_by_entity: "created_by_entity",
            deleted_at: "deleted_at",
            id: "id",
            image: "image",
            name: "name",
            owner_node_id: "owner_node_id",
            setup_steps: [{ key: "value" }],
            spec: { disk_gb: 1, memory_mb: 1, vcpus: 1 },
            status: "status",
            vm_id: "vm_id",
            workdir: "workdir",
        };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/resume")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxResume({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_resume (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/resume")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxResume({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_resume (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/resume")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxResume({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_resume (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/resume")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxResume({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.ConflictError);
    });

    test("sandbox_save_snapshot (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { presigned_url: "presigned_url" };
        const rawResponseBody = { checksum_sha256: "checksum_sha256", size_bytes: 1000000 };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/snapshot")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sandboxes.sandboxSaveSnapshot({
            sandbox_name: "sandbox_name",
            presigned_url: "presigned_url",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_save_snapshot (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { presigned_url: "presigned_url" };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/snapshot")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxSaveSnapshot({
                sandbox_name: "sandbox_name",
                presigned_url: "presigned_url",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_save_snapshot (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { presigned_url: "presigned_url" };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/snapshot")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxSaveSnapshot({
                sandbox_name: "sandbox_name",
                presigned_url: "presigned_url",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_ws_proxy (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().get("/sandboxes/sandbox_name/ws-proxy/1/path").respondWith().statusCode(200).build();

        const response = await client.sandboxes.sandboxWsProxy({
            sandbox_name: "sandbox_name",
            port: 1,
            path: "path",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_ws_proxy (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/ws-proxy/1/path")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxWsProxy({
                sandbox_name: "sandbox_name",
                port: 1,
                path: "path",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_ws_proxy (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/ws-proxy/1/path")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sandboxes.sandboxWsProxy({
                sandbox_name: "sandbox_name",
                port: 1,
                path: "path",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });
});
