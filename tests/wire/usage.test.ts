// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UsageClient", () => {
    test("get_usage_summary (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            tenant_id: "tenant_id",
            period_start: "2024-01-15T09:30:00Z",
            period_end: "2024-01-15T09:30:00Z",
            total_vcpu_seconds: 1,
            total_memory_mb_seconds: 1,
            total_disk_gb_seconds: 1,
            sandbox_count: 1,
            total_cost_cents: 1,
        };

        server.mockEndpoint().get("/usage/me/summary").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.usage.getUsageSummary();
        expect(response).toEqual(rawResponseBody);
    });

    test("get_usage_summary (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server.mockEndpoint().get("/usage/me/summary").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.usage.getUsageSummary();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_usage_summary (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/usage/me/summary").respondWith().statusCode(422).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.usage.getUsageSummary();
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("get_sandbox_usage (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = [
            {
                sandbox_public_id: "sandbox_public_id",
                sandbox_name: "sandbox_name",
                vcpus: 1,
                memory_mb: 1,
                disk_gb: 1,
                billable_seconds: 1,
                cost_cents: 1,
                reality_start: "2024-01-15T09:30:00Z",
                reality_stop: "2024-01-15T09:30:00Z",
                status: "running",
            },
        ];

        server
            .mockEndpoint()
            .get("/usage/me/sandboxes")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.usage.getSandboxUsage();
        expect(response).toEqual(rawResponseBody);
    });

    test("get_sandbox_usage (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .get("/usage/me/sandboxes")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.usage.getSandboxUsage();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_sandbox_usage (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/usage/me/sandboxes")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.usage.getSandboxUsage();
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });
});
