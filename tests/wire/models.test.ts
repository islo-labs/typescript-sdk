// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ModelsClient", () => {
    test("get_model_pricing (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            models: [
                {
                    id: "id",
                    provider: "provider",
                    input_per_1m: 1.1,
                    output_per_1m: 1.1,
                    cache_write_per_1m: 1.1,
                    cache_read_per_1m: 1.1,
                },
            ],
        };

        server.mockEndpoint().get("/models/pricing").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.models.getModelPricing();
        expect(response).toEqual(rawResponseBody);
    });

    test("get_model_pricing (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server.mockEndpoint().get("/models/pricing").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.models.getModelPricing();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_model_pricing (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server.mockEndpoint().get("/models/pricing").respondWith().statusCode(422).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.models.getModelPricing();
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("get_model_pricing (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server.mockEndpoint().get("/models/pricing").respondWith().statusCode(503).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.models.getModelPricing();
        }).rejects.toThrow(IsloApi.ServiceUnavailableError);
    });
});
