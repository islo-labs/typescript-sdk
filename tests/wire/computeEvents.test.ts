// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ComputeEventsClient", () => {
    test("get_compute_event (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            command_id: "command_id",
            action: "action",
            success: true,
            error_message: "error_message",
            started_at: "2024-01-15T09:30:00Z",
            completed_at: "2024-01-15T09:30:00Z",
            result: { type: "empty" },
        };

        server
            .mockEndpoint()
            .get("/compute-events/command_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.computeEvents.getComputeEvent({
            command_id: "command_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("get_compute_event (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/compute-events/command_id")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.computeEvents.getComputeEvent({
                command_id: "command_id",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("get_compute_event (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/compute-events/command_id")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.computeEvents.getComputeEvent({
                command_id: "command_id",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });
});
