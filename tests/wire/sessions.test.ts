// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("SessionsClient", () => {
    test("sandbox_list_sessions (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { sessions: [{ name: "name", started_at_unix_ms: 1000000, status: "connected" }] };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sessions.sandboxListSessions({
            sandbox_name: "sandbox_name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_list_sessions (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxListSessions({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_list_sessions (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxListSessions({
                sandbox_name: "sandbox_name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_create_session (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = { name: "name", status: "status" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/sessions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.sessions.sandboxCreateSession({
            sandbox_name: "sandbox_name",
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sandbox_create_session (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/sessions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxCreateSession({
                sandbox_name: "sandbox_name",
                name: "name",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_create_session (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/sandboxes/sandbox_name/sessions")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxCreateSession({
                sandbox_name: "sandbox_name",
                name: "name",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_kill_session (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server.mockEndpoint().delete("/sandboxes/sandbox_name/sessions/session").respondWith().statusCode(200).build();

        const response = await client.sessions.sandboxKillSession({
            sandbox_name: "sandbox_name",
            session: "session",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_kill_session (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .delete("/sandboxes/sandbox_name/sessions/session")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxKillSession({
                sandbox_name: "sandbox_name",
                session: "session",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_kill_session (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .delete("/sandboxes/sandbox_name/sessions/session")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxKillSession({
                sandbox_name: "sandbox_name",
                session: "session",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });

    test("sandbox_attach_session (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions/session/attach")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.sessions.sandboxAttachSession({
            sandbox_name: "sandbox_name",
            session: "session",
        });
        expect(response).toEqual(undefined);
    });

    test("sandbox_attach_session (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions/session/attach")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxAttachSession({
                sandbox_name: "sandbox_name",
                session: "session",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("sandbox_attach_session (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .get("/sandboxes/sandbox_name/sessions/session/attach")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.sessions.sandboxAttachSession({
                sandbox_name: "sandbox_name",
                session: "session",
            });
        }).rejects.toThrow(IsloApi.NotFoundError);
    });
});
