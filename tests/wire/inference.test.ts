// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("InferenceClient", () => {
    test("list_inference_models (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            models: [
                {
                    id: "id",
                    upstream_provider: "fireworks",
                    upstream_model_id: "upstream_model_id",
                    display_name: "display_name",
                    aliases: ["aliases"],
                    client_apis: ["openai_chat_completions"],
                    enabled: true,
                    input_cents_per_1m_tokens: "input_cents_per_1m_tokens",
                    cached_input_cents_per_1m_tokens: "cached_input_cents_per_1m_tokens",
                    output_cents_per_1m_tokens: "output_cents_per_1m_tokens",
                },
            ],
        };

        server.mockEndpoint().get("/inference/models").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.inference.listInferenceModels();
        expect(response).toEqual(rawResponseBody);
    });

    test("list_inference_models (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server.mockEndpoint().get("/inference/models").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.inference.listInferenceModels();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("list_inference_models (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {};

        server.mockEndpoint().get("/inference/models").respondWith().statusCode(422).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.inference.listInferenceModels();
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });
});
