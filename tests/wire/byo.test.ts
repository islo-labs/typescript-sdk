// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ByoClient", () => {
    test("get_byo_inference_status (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = {
            statuses: [
                {
                    source_kind: "databricks",
                    state: "not_connected",
                    provider_name: "provider_name",
                    message: "message",
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/byo/inference/status")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.byo.getByoInferenceStatus();
        expect(response).toEqual(rawResponseBody);
    });

    test("get_byo_inference_status (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .get("/byo/inference/status")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.getByoInferenceStatus();
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("get_byo_inference_status (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/byo/inference/status")
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.getByoInferenceStatus();
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("start_byo_inference_setup (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { source_kind: "databricks" };
        const rawResponseBody = {
            setup_session_id: "setup_session_id",
            connection_id: "connection_id",
            source_kind: "databricks",
            setup_mode: "managed_setup_link",
            redirect_url: "redirect_url",
        };

        server
            .mockEndpoint()
            .post("/byo/inference/start-setup")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.byo.startByoInferenceSetup({
            source_kind: "databricks",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("start_byo_inference_setup (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { source_kind: "databricks" };
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .post("/byo/inference/start-setup")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.startByoInferenceSetup({
                source_kind: "databricks",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("start_byo_inference_setup (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { source_kind: "databricks" };
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .post("/byo/inference/start-setup")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.startByoInferenceSetup({
                source_kind: "databricks",
            });
        }).rejects.toThrow(IsloApi.ForbiddenError);
    });

    test("start_byo_inference_setup (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { source_kind: "databricks" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/byo/inference/start-setup")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.startByoInferenceSetup({
                source_kind: "databricks",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });

    test("start_byo_inference_setup (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new Islo({
            maxRetries: 0,
            apiKey: "test",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { source_kind: "databricks" };
        const rawResponseBody = { code: "AUTH_REQUIRED", message: "message" };

        server
            .mockEndpoint()
            .post("/byo/inference/start-setup")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(502)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.byo.startByoInferenceSetup({
                source_kind: "databricks",
            });
        }).rejects.toThrow(IsloApi.BadGatewayError);
    });
});
