// This file was auto-generated by Fern from our API Definition.

import * as IsloApi from "../../src/api/index";
import { Islo } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";
import { mockIsloAccessKey } from "./mockAuth";

describe("AuthClient", () => {
    test("exchange_access_key (1)", async () => {
        const server = mockServerPool.createServer();
        mockIsloAccessKey(server);

        const client = new Islo({
            maxRetries: 0,
            clientId: "access_key",
            clientSecret: "test_client_secret",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { access_key: "access_key" };
        const rawResponseBody = { session_token: "session_token", expires_in: 1 };

        server
            .mockEndpoint()
            .post("/auth/token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.auth.exchangeAccessKey({
            access_key: "access_key",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("exchange_access_key (2)", async () => {
        const server = mockServerPool.createServer();
        mockIsloAccessKey(server);

        const client = new Islo({
            maxRetries: 0,
            clientId: "access_key",
            clientSecret: "test_client_secret",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { access_key: "access_key" };
        const rawResponseBody = { code: "UNAUTHORIZED", message: "message" };

        server
            .mockEndpoint()
            .post("/auth/token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.exchangeAccessKey({
                access_key: "access_key",
            });
        }).rejects.toThrow(IsloApi.UnauthorizedError);
    });

    test("exchange_access_key (3)", async () => {
        const server = mockServerPool.createServer();
        mockIsloAccessKey(server);

        const client = new Islo({
            maxRetries: 0,
            clientId: "access_key",
            clientSecret: "test_client_secret",
            environment: { control: server.baseUrl, compute: server.baseUrl },
        });
        const rawRequestBody = { access_key: "access_key" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/auth/token")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.auth.exchangeAccessKey({
                access_key: "access_key",
            });
        }).rejects.toThrow(IsloApi.UnprocessableEntityError);
    });
});
