// This file was auto-generated by Fern from our API Definition.

import type * as IsloApi from "../index.js";

export type SandboxConfigInit =
    | IsloApi.SandboxConfigInit.Custom
    | IsloApi.SandboxConfigInit.Full
    | IsloApi.SandboxConfigInit.Minimal;

export namespace SandboxConfigInit {
    export interface Custom {
        type: "custom";
        /**
         * Block until capabilities are fully ready (e.g. sshd accepts
         * connections, dockerd responds to API calls). Defaults to false.
         */
        await_ready?: boolean | undefined;
        capabilities: IsloApi.InitCapability[];
    }

    export interface Full {
        type: "full";
    }

    export interface Minimal {
        type: "minimal";
    }
}
