// This file was auto-generated by Fern from our API Definition.

/**
 * Response when creating a new API key (includes the secret key).
 */
export interface CreateApiKeyResponse {
    id: string;
    name: string;
    is_active: boolean;
    created_at: string;
    expires_at: string | null;
    created_by_user_id: string;
    /** The API key secret - only shown once at creation time */
    key: string;
}
